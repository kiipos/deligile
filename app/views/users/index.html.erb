<ul id="users">
  <% @users.each() do | user | %>
    <li>
      <%= user.sign_in_id %> (<%= user.email %>)
      <%= link_to( '삭제', user_path( user.id ), :method => :delete, 
        :confirm => '삭제하시겠습니까?' )%>
    </li>
  <% end %>
</ul>

<p>
  <%= link_to( '새 사용자', new_user_path )%>
</p>
